import assert from "assert";
import fetch from "node-fetch";
import { HYPO } from "./src/libs/HYPO/HYPO";

describe("XXX", () => {
  it("XXXXX", function (done) {
    const x = new HYPO({
      templatePath: "menubutton.template.html",
      data: {},
    });
  });
});
